const islandPerimeter = require('./islandPerimeter');

describe('Island Perimeter problem', () => {

    it('Should be a function', () => {
        expect(typeof islandPerimeter).toBe('function');
    });

});